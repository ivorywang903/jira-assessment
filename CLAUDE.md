# CLAUDE.md

## 重要規則

### README 同步
每次修改以下任一項目後，**必須同步更新 README.md**，不需要等使用者提醒：

- `src/` 下任何功能新增、修改、刪除
- `supabase/migrations/` 新增 migration
- `.github/workflows/` 修改 CI 流程
- `.env.example` 新增或移除環境變數
- 登入機制、權限邏輯異動

更新範圍：受影響的章節（功能說明、快速開始、初始設定、CI/CD、專案結構、注意事項）。
